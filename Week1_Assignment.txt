Week 1

1) GitHub
  
 i) Create Repository and add its name and Description in Github.
    git init

ii) git remote add origin master https://github.com/Neeraj-2526/PJP-2.0.git

iii) git branch Feature_branch
    git branch Dev
    git branch QA
    git branch Production
    git branch Delivery

iv) git push origin Feature_branch
     git push origin Dev
     git push origin QA
     git push origin Production
     git push origin Delivery

v) git add -a
    git add -A

vi) cat Hello.txt
     Update the file
     git add -A
     git commit -m”......”
     Git push origin Feature_branch

vii) Create pull request
     Change base branch to Dev and then confirm pull request

viii) git Checkout file_name

ix) git rebase <branch or commit hash>
x) git rebase -i <branch or commit hash>
xi) git branch -f <source branch> <destination branch or commit hash>



 
 


